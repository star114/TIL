# change-finder-default-sort

* Open the finder window and hit cmd+j.
* Select the settings you want and then at the bottom hit Use As Defaults.

## ALL folders?

```
$ sudo find / -name ".DS_Store" -exec rm {} \;
$ killall Finder
```
